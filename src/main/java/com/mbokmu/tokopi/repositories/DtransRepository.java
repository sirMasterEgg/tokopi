package com.mbokmu.tokopi.repositories;

import com.mbokmu.tokopi.models.Dtrans;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DtransRepository extends JpaRepository<Dtrans, Integer> {
}
